package jamuz.process.check;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MP3gainTest {
    @Test
    void shouldBeLoadable() {
        assertNotNull(MP3gain.class);
    }
}
