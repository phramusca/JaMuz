package jamuz.remote;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class DialogClientInfoTest {
    @Test
    void shouldBeLoadableClass() {
        assertNotNull(DialogClientInfo.class);
    }
}
